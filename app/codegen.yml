overwrite: true
schema: "http://localhost:5001/cms-uoa/us-central1/graph"
documents: null
generates:
    src/types/types.ts:
        plugins:
            - typescript
    src/types/schema.json:
        plugins:
            - fragment-matcher
